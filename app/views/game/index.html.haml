%h1 List of games in the database

%table
  %thead
    %tr
      %th Title
      %th Year Published
      %th Number of Players
  %tbody
    - @games.each do |game|
      %tr
        %td= game.name
        %td= game.year_published
        %td= game.player_range_string
